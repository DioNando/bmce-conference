@props(['class' => ''])

<div class="rounded-md overflow-hidden">
    <img class="w-auto mx-auto block {{ $class }}"
        src="{{ asset('img/bmce-logo.jpeg') }}" alt="BMCE Capital">
    {{-- <h2 class="w-auto mx-auto block text-center text-3xl font-bold text-blue-800">{{ config('app.name') }}</h2> --}}
</div>
